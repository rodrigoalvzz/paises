-- Crea una tabla llamada "country_data"
CREATE TABLE country_data (
    country_name VARCHAR(255),
    capital VARCHAR(255),
    population INT,
    region VARCHAR(255)
);

SELECT * FROM country_data;